//tc-O(n)

int linearSearch(int n, int num, vector<int> &a)
{
    // Write your code here.
    int i;
    for(i=0;i<n;i++)
    {
        if(a[i]==num)
        break;
    }
    if(i==n)
    return -1;
    else
    return i;
}
